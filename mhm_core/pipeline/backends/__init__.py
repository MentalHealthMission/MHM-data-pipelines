"""Optional pipeline backend implementations.

The core pipeline package owns backend protocols and selection logic. Concrete
storage implementations live under this namespace so callers can opt into a
backend without making it part of the minimal runner contract.
"""

__all__ = []
